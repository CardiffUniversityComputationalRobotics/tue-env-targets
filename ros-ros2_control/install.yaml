- type: ros
  source:
    type: system
    name: ros2-control
  # Previously source-pinned to 2.45.0 to satisfy older franka_ros2 on real
  # hardware; humble apt now ships >= 2.53.1 on the same release line, which
  # is ABI-compatible. Source-pinning was also adding ~12 sub-packages to the
  # colcon build. Reactivate the git override below if a future franka_ros2
  # version constraint requires it.
  # humble:
  #   source:
  #     type: git
  #     url: https://github.com/ros-controls/ros2_control.git
  #     # version: 2.25.0
  #     version: 2.45.0