- type: ros
  humble:
    source:
      # real-robot path (older franka_ros2 v0.1.15 needs source-pinned ros2_control):
      # type: git
      # url: https://github.com/ros-controls/ros2_control.git
      # version: 2.45.0
      # sim path (apt 2.53.1 — works with franka_ros2 v2.1.0):
      type: system
      name: ros2-control
  default:
    source:
      type: system
      name: ros2-control