- type: ros
  source:
    type: system
    name: ros2-controllers
  # Previously source-pinned to 2.45.0 to satisfy older franka_ros2 on real
  # hardware; humble apt now ships >= 2.52.1 on the same release line, which
  # is ABI-compatible. Source-pinning was also adding ~24 sub-packages to the
  # colcon build, including a copy of joint_trajectory_controller that
  # collided with franka_ros2's vendored 3.3.0 fork of the same package.
  # Reactivate the git override below if a future franka_ros2 version
  # constraint requires it.
  # humble:
  #   source:
  #     type: git
  #     url: https://github.com/ros-controls/ros2_controllers.git
  #     # version: 2.25.0
  #     version: 2.45.0